#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char str[100];
	int len;
	clrscr();
	printf("enter value of string:");
	scanf("%s",&str);
	printf("entered string: %s\n",str);
	len=strlen(str);
	printf("enter length of string: %d",len);
	getch();
}